# ============================================
# Render.com Blueprint — Forever Valentine
# ============================================

services:
  - type: web
    name: forever-valentine
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    envVars:
      - key: APP_ENV
        value: prod
      - key: APP_SECRET
        generateValue: true
